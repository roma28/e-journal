<% provide :title, @user.name+" | " %>

<%= @user.name%> <%= @user.login %>
<%= link_to(raw('Изменить  <i class="fa fa-pencil"></i>'), edit_user_path(@user), class:"btn btn-primary btn-sm") %> &nbsp&nbsp&nbsp&nbsp
<%= link_to(raw('Удалить <i class="fa fa-trash"></i>'), @user, method: :delete, data: { confirm: "Вы уверены" }, class: "btn btn-primary btn-sm") if !current_user?(@user) %></td>