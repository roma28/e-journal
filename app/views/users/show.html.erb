<% provide :title, @user.name+" | " %>

<%= @user.name%> <%= @user.login %>
<%= link_to(raw('Изменить  <span class="glyphicon glyphicon-pencil"></span>'), edit_user_path(@user), class:"btn btn-primary btn-sm") %> &nbsp&nbsp&nbsp&nbsp
<%= link_to(raw('Удалить <span class="glyphicon glyphicon-trash"></span>'), @user, method: :delete, data: { confirm: "Вы уверены" }, class: "btn btn-primary btn-sm") if !current_user?(@user) %></td>